CREATE TABLE [dbo].[TBTIPCTO] (
    [TIPCTO] TINYINT      NOT NULL,
    [DSCCTO] VARCHAR (30) NOT NULL,
    [STAREC] TINYINT      DEFAULT ((1)) NOT NULL,
    [DATCAD] DATETIME     DEFAULT (getdate()) NOT NULL,
    [DATUPD] DATETIME     DEFAULT (getdate()) NULL,
    [UPDUSU] INT          DEFAULT ((0)) NOT NULL,
    CONSTRAINT [PK_TBTIPCTO] PRIMARY KEY CLUSTERED ([TIPCTO] ASC)
);
